{
  "name": "transmed-invoice-processing-system",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p .vercel/output/static && cp index.html .vercel/output/static/index.html"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  }
}
